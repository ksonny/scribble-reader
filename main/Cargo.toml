[package]
name = "scribble-reader"
version = "0.1.0"
authors = ["ksonny <ksonny@lotrax.org>"]
edition = "2024"
publish = false

[dependencies]
chrono = { version = "0.4.42", default-features = false, features = ["alloc", "clock", "now", "std", "serde"] }
clock = { version = "0.4.0", default-features = false }
config = { version = "0.15.17", default-features = false, features = ["toml"] }
dotenv = "0.15.0"
egui = "0.32.3"
egui-wgpu = "0.32.3"
egui-winit = "0.32.3"
egui_extras = { version = "0.32.3", features = ["svg", "image"] }
env_logger = "0.11.8"
expand-tilde = "0.6.1"
image = { version = "0.25.8", features = ["png", "jpeg", "ico"] }
lazy_static = "1.5.0"
log = "0.4.28"
lucide-icons = "0.544.0"
now = { version = "0.1.3", default-features = false }
pollster = "0.4.0"
rayon = "1.11.0"
rbook = { version = "0.6.6", default-features = false }
rusqlite = { version = "0.37.0", features = ["bundled", "blob", "rusqlite-macros", "chrono"] }
rusqlite_migration = "2.3.0"
serde = { version = "1.0.226", features = ["derive"] }
serde_rusqlite = "0.40.0"
svg = "0.18.0"
thiserror = "2.0.16"
xdg = "3.0.0"

[target.'cfg(not(target_os = "android"))'.dependencies]
winit = { version = "0.30.12" }

[target.'cfg(target_os = "android")'.dependencies]
android_logger = "0.15.1"
winit = { version = "0.30.12", features = ["android-native-activity"] }

[[bin]]
# Rename the bin to avoid https://github.com/rust-lang/cargo/issues/6313
name = "scribble-reader-desktop"
path = "src/main.rs"

[lib]
crate-type = ["cdylib", "lib"]

[package.metadata.android.signing.release]
path = "./path/to/KeyStoreFile.jks"
keystore_password = "password"

[package.metadata.android.signing.dev-opt]
path = "/home/ksonny/.android/debug.keystore"
keystore_password = "android"

[package.metadata.android]
build_targets = ["aarch64-linux-android"]
resources = "resources"

[package.metadata.android.sdk]
min_sdk_version = 23
target_sdk_version = 35

[[package.metadata.android.uses_feature]]
name = "android.hardware.vulkan.level"
required = true
version = 1

[[package.metadata.android.queries.provider]]
authorities = "org.khronos.openxr.runtime_broker;org.khronos.openxr.system_runtime_broker"
# Note: The `name` attribute is normally not required for a queries provider, but is non-optional
# as a workaround for aapt throwing errors about missing `android:name` attribute.
# This will be made optional if/when cargo-apk migrates to aapt2.
name = "org.khronos.openxr"

[package.metadata.android.application]
theme = "@android:style/Theme.DeviceDefault.NoActionBar.Fullscreen"
label = "Scribble Reader"
extract_native_libs = true
uses_cleartext_traffic = true

