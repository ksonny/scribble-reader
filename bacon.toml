#:schema https://dystroy.org/bacon/.bacon.schema.json

# This is a configuration file for the bacon tool
#
# Complete help on configuration: https://dystroy.org/bacon/config/
#
# You may check the current default at
#   https://github.com/Canop/bacon/blob/main/defaults/default-bacon.toml

default_job = "check"
env.CARGO_TERM_COLOR = "always"

[jobs.check]
command = ["cargo", "check"]
need_stdout = false

[jobs.check-all]
command = ["cargo", "check", "--all-targets"]
need_stdout = false

# Run clippy on the default target
[jobs.clippy]
command = ["cargo", "clippy"]
need_stdout = false

[jobs.clippy-all]
command = ["cargo", "clippy", "--all-targets"]
need_stdout = false

[jobs.pedantic]
command = [
	"cargo", "clippy",
	"--",
	"-W", "clippy::pedantic",
]
need_stdout = false

[jobs.test]
command = ["cargo", "test"]
need_stdout = true

[jobs.ndk-check]
command = ["cargo", "ndk", "--target", "aarch64-linux-android", "check", "--package", "scribble-reader-android"]
need_stdout = false

[jobs.ndk-build]
command = [
	"cargo", "ndk", "--target", "aarch64-linux-android", "--target", "x86_64-linux-android",
	"build", "--package", "scribble-reader-android", "-o", "./app-android/app/src/main/jniLibs",
]
need_stdout = false

# You may define here keybindings that would be specific to
# a project, for example a shortcut to launch a specific job.
# Shortcuts to internal functions (scrolling, toggling, etc.)
# should go in your personal global prefs.toml file instead.
[keybindings]
# alt-m = "job:my-job"
c = "job:clippy-all" # comment this to have 'c' run clippy on only the default target
p = "job:pedantic"

alt-c = "job:ndk-check"
b = "job:ndk-build"
